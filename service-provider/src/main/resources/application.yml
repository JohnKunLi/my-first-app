server:
  port: 8081

spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # 下面两个是默认值，可以不写，但写上更保险
        enabled: true
        register-enabled: true